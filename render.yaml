services:   
  - type: web
    name: livekit-meet  
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
      envVars:
            - key: NODE_VERSION
            value: "18"
